__pycache__/
*.pyc
*.bak_*
*.bak
outputs/
data/
python/
*.log
config.json
chrome_debug_data_*/
venv/
test_uc.py
CODEBASE_STATE.md
*.ps1
*.lnk
test2/
test/
test3/
_release_clean/

# Grok会話DB・ベクター索引（518MB。会話内容そのものなので絶対に公開しない）
# コードは grok_history/scripts/ に置くが、データは各自の環境で用意する。
grok_history/runtime/
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.faiss

# 取り込み待ちのGrokエクスポートZIP（中身は会話そのもの）
grok_history/incoming/*
!grok_history/incoming/README.txt
*.zip

# RunPod会話スレッドの控え（実行時に作られる環境固有の状態）
runpod_chat_state.json

# 環境依存の一覧（曲・体位・衣装）は各自で用意する
appends/
