# Windowsのバッチファイルは改行がCRLFでないと正しく動かない。
# LFのままだと行の区切りを誤認し、変数が展開されないまま即座に落ちる。
#
# GitHubの「Download ZIP」は、リポジトリに保存されている中身を
# そのまま固めて渡す。つまりリポジトリ内がLFだと、zipで配布した時点で壊れる。
#
# text eol=crlf では「作業ツリーへ出す時だけCRLF、保存はLF」になり、
# zip配布では効かない。-text で変換自体を止め、CRLFのまま保存させる。
*.bat -text
*.cmd -text
*.ps1 -text
